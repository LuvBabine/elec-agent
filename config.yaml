llm:
  provider: ollama          # ollama | openai | anthropic
  model: qwen2.5vl          # vision-capable model (qwen2.5vl, llava, gpt-4o)
  api_key: null             # required for openai/anthropic

rules:
  standard: NFC15100        # NFC15100 | IEC60364 | NEC2023 | BS7671
  strictness: normal        # normal | strict

output:
  format: pdf               # pdf | txt | json
  language: fr              # fr | en
